import React from 'react'
import {
    View,
    ActivityIndicator
} from 'react-native'

const Loading = () => (
    <View>
        <ActivityIndicator size='large' color='#000'/>
    </View>
)

export default Loading
